apiVersion: v1
kind: Secret
metadata:
  name: payments-worker-secret
type: Opaque
stringData:
  # Reservado para futuras configurações sensíveis do worker
  # (ex.: API keys, tokens externos, etc.)
  PLACEHOLDER: "<FUTURE_SECRET_VALUE>"
